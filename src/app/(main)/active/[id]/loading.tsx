import LoadingDualSpinner from '@/components/loading/DualSpinner';

export default function ActiveTestLoading() {
  return (
    <div className='loading'>
      <LoadingDualSpinner />
    </div>
  );
}
